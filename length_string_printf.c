/* WAP to calculate the length of String using printf function */

#include<stdio.h>
int main() {

    printf("Lenght of the string is %d", printf("iNeuron\n")); 
    // printf function has the return type as int, so it returns the 
    // total number of characters written within double quotes.
    
/* OR 
   int length = printf("Onkar student @iNeuron");
   printf("\nLength of the string is %d",length);  
*/
   return 0;
}